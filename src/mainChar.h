#include <iostream>
#include <string.h>

# ifndef MainChar_h
# define MainChar_h
# endif 

class MainChar
{
    pulbic:
    MainChar();     //constructor of the class
    char Symb;      //Symbol of a character
    int x, y;       //Coordinates of a character
    char Name;      // Name of a character 
                    //(also as the name of savings if players creates more than one character)
}
